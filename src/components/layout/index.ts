/**
 * Layout components for consistent responsive behavior
 * Implements the layout optimization specifications
 */

export {
  ScreenWrap,
  PageWrap,
  ContentWrap,
  CardWrap,
  GridWrap
} from './screen-wrap';

// Re-export types if needed
export type {
  // Add any exported types here if you create them later
} from './screen-wrap';